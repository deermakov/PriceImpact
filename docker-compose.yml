version: '3.8'

services:
  price-impact-analyzer:
    build: .
    volumes:
      - .:/app
    environment:
      - INPUT_FILE=/app/2026.08.10.txt
      - OUTPUT_FILE=/app/result_plot.png
      - TIME_STEP_SEC=60
      - PRICE_STEP=10
      - PERCENTILE_GRID_SIZE=1